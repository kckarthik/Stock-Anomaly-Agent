name: stock_pipeline
version: "1.0.0"
config-version: 2

profile: stock_pipeline

model-paths:  ["models"]
test-paths:   ["tests"]
seed-paths:   ["seeds"]
macro-paths:  ["macros"]

target-path:  "target"
clean-targets: ["target"]

models:
  stock_pipeline:
    staging:
      +materialized: view
    gold:
      +materialized: table
      +schema: gold
